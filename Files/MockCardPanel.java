import javax.swing.*;

/**
 * Mockup of selectable cards needed to implement swapping with the results panel
 *
 * @author Owen McManus
 * @version 1
 */
public class MockCardPanel extends JPanel{
    public MockCardPanel(){
        JLabel titleLabel = new JLabel("Cards");
        add(titleLabel);
    }
}
